<?php
// זהו תבנית בלבד — להעתיק לקובץ בשם config.php (לא נכנס ל-git) ולמלא ערכים אמיתיים.
// cp config.sample.php config.php

// הסיסמה לגישה למערכת — בחרי משהו שאת יכולה להקליד בנוחות מהנייד
$PASSWORD = 'CHANGE-ME';

// טוקן קריאה-בלבד לפיד היומן (ICS). משמש רק לצפייה בהדרכות ביומן גוגל,
// לא נותן הרשאות עריכה. אפשר להחליף אותו כדי לבטל מנויים קיימים.
$FEED_TOKEN = 'CHANGE-ME-TOO';

// מיקום קובץ הנתונים
$DB_PATH = __DIR__ . '/tasks.db';
